import { registerType } from "../types/registerTypes";
import axios from "@/api/axios";

export default function useRegister() {
  const handleRegister = async (registerData: registerType) => {};

  return handleRegister;
}
