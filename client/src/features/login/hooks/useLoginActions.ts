const useLoginActions = () => {
  const handleLogin = async (
    prevState: unknown,
    formData: FormData
  ): Promise<string> => {
    return "Error";
  };
  return handleLogin;
};

export default useLoginActions;
